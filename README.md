# deno-bls-runtime
a secure runtime for blockless based written with Deno instead of Rust.

## usage

`bls-runtime` and the source files are currently assumed to be relative of eachother this early.

```bash
bls-runtime ${target}
bls-runtime test/hello.ts
bls-runtime test/hello.wasm
```